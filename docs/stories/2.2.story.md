# Story 2.2: Admin Produtos CRUD

---

## Status

**Draft**

---

## Executor Assignment

executor: "@dev"
quality_gate: "@pm"
quality_gate_tools: ["manual-review", "browser-testing"]

---

## Story

**As a** administrador da plataforma,
**I want** gerenciar o catalogo de produtos com listagem, criacao, edicao e exclusao,
**so that** eu possa manter o catalogo atualizado sem acesso direto ao banco de dados.

---

## Epic

**Admin Panel novob2b**

## Prioridade

**Alta**

## Estimativa

**G** (Grande)

## Proprietario

@dev

---

## Descricao

Implementar o modulo completo de gestao de produtos no painel administrativo. Inclui listagem paginada com busca e filtros, modal de criacao/edicao, toggle ativo/inativo, soft-delete, bulk actions e upload de imagem para Supabase Storage.

### Schema do Produto (referencia `database.ts`)
```typescript
interface Product {
  id: string
  name: string
  description: string | null
  price: number
  unit: ProductUnit  // 'un' | 'kg' | 'cx' | 'maco' | 'dz'
  image_url: string | null
  category_id: string | null
  is_active: boolean
  deleted_at: string | null  // soft-delete
  display_id: number         // ID amigavel auto-increment
  created_at: string
  categories?: Category
}
```

### Funcionalidades
1. **Listagem** - Tabela paginada (20 por pagina) com colunas: ID, Imagem (thumbnail), Nome, Preco, Unidade, Categoria, Status, Acoes
2. **Busca** - Por nome ou display_id
3. **Filtros** - Status (ativo/inativo/todos), Categoria (dropdown)
4. **Criar Produto** - Modal com formulario completo + upload de imagem
5. **Editar Produto** - Mesmo modal pre-populado
6. **Toggle Ativo/Inativo** - Switch inline na tabela
7. **Soft-Delete** - Setar `deleted_at` (nao excluir do banco)
8. **Bulk Actions** - Checkbox de selecao + acoes: ativar, desativar, excluir em massa
9. **Upload de Imagem** - Upload para Supabase Storage bucket `product-images`

---

## Criterios de Aceite

- [ ] CA1: Pagina renderiza em `/admin/produtos`
- [ ] CA2: Tabela lista produtos com colunas: Display ID (#), Thumbnail, Nome, Preco (R$), Unidade, Categoria, Status (badge), Acoes
- [ ] CA3: Paginacao funcional (20 itens por pagina) com indicador de pagina atual e total
- [ ] CA4: Campo de busca filtra por nome (parcial, case-insensitive) e por display_id (numerico exato)
- [ ] CA5: Filtro por status funciona (Ativo / Inativo / Todos) — default "Ativo"
- [ ] CA6: Filtro por categoria funciona (dropdown com todas as categorias + "Todas")
- [ ] CA7: Botao "Novo Produto" abre modal de criacao
- [ ] CA8: Modal de criacao tem campos: Nome*, Preco*, Unidade* (select), Categoria (select), Descricao (textarea), Imagem (upload)
- [ ] CA9: Validacao de formulario: nome obrigatorio (min 2 chars), preco obrigatorio (> 0), unidade obrigatoria
- [ ] CA10: Clicar em "Editar" na linha abre o mesmo modal pre-populado com dados do produto
- [ ] CA11: Toggle ativo/inativo funciona inline na tabela com feedback visual imediato (optimistic update)
- [ ] CA12: Acao "Excluir" faz soft-delete (seta `deleted_at = now()`) com dialog de confirmacao
- [ ] CA13: Checkbox de selecao por linha + "Selecionar Todos" funciona
- [ ] CA14: Bulk actions (Ativar, Desativar, Excluir) aparecem quando ha selecao e funcionam corretamente
- [ ] CA15: Upload de imagem envia para Supabase Storage e salva `image_url` no produto
- [ ] CA16: Preview da imagem no modal antes do upload
- [ ] CA17: Produtos com `deleted_at` nao aparecem na listagem padrao
- [ ] CA18: Loading states (skeleton) durante carregamento
- [ ] CA19: Toast de sucesso/erro nas operacoes CRUD
- [ ] CA20: Layout responsivo

---

## Tarefas

- [ ] T1: Criar service `src/features/admin/services/products.ts`:
  - `fetchProducts(params: { page, pageSize, search, status, categoryId })` — listagem paginada com filtros
  - `fetchProductById(id: string)` — produto individual
  - `createProduct(data: ProductInsert)` — criar produto
  - `updateProduct(id: string, data: ProductUpdate)` — editar produto
  - `toggleProductActive(id: string, isActive: boolean)` — toggle status
  - `softDeleteProduct(id: string)` — soft-delete
  - `bulkUpdateProducts(ids: string[], data: Partial<Product>)` — bulk update
  - `uploadProductImage(file: File)` — upload para Supabase Storage
  - `deleteProductImage(url: string)` — remover imagem antiga
- [ ] T2: Criar service `src/features/admin/services/categories.ts`:
  - `fetchAllCategories()` — lista todas categorias (para dropdown)
- [ ] T3: Criar hook `src/features/admin/hooks/useProducts.ts` — gerencia estado da listagem, paginacao, filtros e busca
- [ ] T4: Criar componente `src/features/admin/components/ProductsTable.tsx` — tabela com selecao, sorting e acoes inline
- [ ] T5: Criar componente `src/features/admin/components/ProductFormModal.tsx` — modal de criacao/edicao com validacao Zod
- [ ] T6: Criar componente `src/features/admin/components/ProductFilters.tsx` — barra de filtros (busca + status + categoria)
- [ ] T7: Criar componente `src/features/admin/components/BulkActionsBar.tsx` — barra flutuante com acoes em massa
- [ ] T8: Criar componente `src/features/admin/components/ImageUpload.tsx` — upload com preview e progress
- [ ] T9: Criar pagina `src/features/admin/pages/ProductsPage.tsx` — composicao dos componentes
- [ ] T10: Registrar rota `/admin/produtos` no `App.tsx`
- [ ] T11: Configurar Supabase Storage bucket `product-images` (se nao existir)
- [ ] T12: Adicionar RLS policy para admin CRUD em `products` (verificar se ja existe)
- [ ] T13: Testar CRUD completo com dados reais
- [ ] T14: Testar bulk actions com multiplas selecoes
- [ ] T15: Testar upload de imagem (formatos: jpg, png, webp; max: 2MB)
- [ ] T16: Verificar responsividade

---

## Arquivos Esperados

- `src/features/admin/services/products.ts`
- `src/features/admin/services/categories.ts`
- `src/features/admin/hooks/useProducts.ts`
- `src/features/admin/components/ProductsTable.tsx`
- `src/features/admin/components/ProductFormModal.tsx`
- `src/features/admin/components/ProductFilters.tsx`
- `src/features/admin/components/BulkActionsBar.tsx`
- `src/features/admin/components/ImageUpload.tsx`
- `src/features/admin/pages/ProductsPage.tsx`
- `src/App.tsx` (modificado — nova rota)

---

## Dependencias

- Story 2.1 (Dashboard) — nao bloqueia, pode ser desenvolvida em paralelo
- AdminLayout ja implementado
- Schema de tipos: `Product`, `Category`, `ProductUnit`
- Supabase client configurado
- Componentes shadcn/ui: Table, Dialog, Input, Select, Button, Badge, Checkbox, Switch, Skeleton, Textarea
- Zod para validacao de formulario

---

## Notas Tecnicas

- **Paginacao Supabase**: Usar `.range(from, to)` com `{ count: 'exact' }` para obter total. Calcular `from = (page - 1) * pageSize` e `to = from + pageSize - 1`.
- **Busca**: Usar `.ilike('name', '%search%')` para busca por nome. Para display_id, usar `.eq('display_id', Number(search))` quando input for numerico.
- **Soft-Delete**: Filtrar `deleted_at IS NULL` por padrao. Nunca fazer DELETE real.
- **Upload Storage**: Usar `supabase.storage.from('product-images').upload(path, file)`. Gerar path unico com `crypto.randomUUID()`. Obter URL publica com `getPublicUrl()`.
- **Optimistic Updates**: Para toggle ativo/inativo, atualizar UI imediatamente e reverter em caso de erro.
- **Display ID**: Campo `display_id` e auto-increment no banco. Nao permitir edicao pelo admin. Exibir como `#001`, `#002` etc.
- **Unidades**: Exibir label legivel — un: "Unidade", kg: "Quilo", cx: "Caixa", maco: "Maco", dz: "Duzia".
- **Imagem**: Limitar a 2MB. Aceitar jpg, png, webp. Ao editar, se nova imagem for enviada, deletar a antiga do Storage.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-25 | 1.0.0 | Story criada pelo PM | @pm (Synkra AIOS) |

---

## Dev Agent Record

### Agent Model Used

_A preencher durante implementacao_

### Debug Log References

_A preencher durante implementacao_

### Completion Notes List

_A preencher durante implementacao_

### File List

_A preencher durante implementacao_

---

## QA Results

_A preencher durante QA review_
